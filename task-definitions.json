{
  "TASK-000-1": {
    "steps": []
  },
  "TASK-000-2": {
    "steps": [
      "cd apps/portfolio-react",
      "npm install -D tailwindcss postcss autoprefixer",
      "npx tailwindcss init -p",
      "echo '@tailwind base;\\n@tailwind components;\\n@tailwind utilities;' > src/index.css",
      "node -e \"const fs=require('fs');const config=`/** @type {import('tailwindcss').Config} */\\nexport default { content: ['./index.html','./src/**/*.{js,ts,jsx,tsx}'], theme: { extend: {} }, plugins: [] }`; fs.writeFileSync('tailwind.config.js', config);\"",
      "echo 'import { defineConfig } from \\\"vite\\\"; import react from \\\"@vitejs/plugin-react\\\"; export default defineConfig({ plugins: [react()] });' > vite.config.ts"
    ]
  },
  "TASK-000-3": {
    "steps": [
      "cd apps/portfolio-react",
      "npm install -D @typescript-eslint/eslint-plugin @typescript-eslint/parser eslint eslint-plugin-react-hooks eslint-plugin-react-refresh",
      "npx eslint --init"
    ]
  }
}